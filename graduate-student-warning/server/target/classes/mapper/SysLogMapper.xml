<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.graduation.mesume.dao.SysLogMapper">

    <select id="countCnt" parameterType="int" resultType="int">
        select count(*) from sys_log where `type` = #{type}
    </select>

    <select id="countTodayCnt" resultType="int">
        select count(*) from sys_log where `type` = #{type} and `create_time` >= #{time}
    </select>
    <select id="countCntByMonth" resultType="map">
       SELECT DATE_FORMAT(create_time,'%m') as `month`,count(1) as cnt FROM `sys_log` where type = #{type} GROUP BY DATE_FORMAT(create_time,'%m');
    </select>
</mapper>
